<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sagiKor1193.android.fitracker">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.STORAGE"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_fitracker_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_fitracker_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AddStudentActivity"
            android:label="Add Student"/>
            <meta-data android:name="android.app.default_searchable"
            android:value=".SearchableActivity" />


        <activity android:name=".ViewDataActivity"
            android:label="Students Results" />

        <activity android:name=".UploadExcelFileActivity"
            android:label="Import Excel File"/>

        <activity android:name=".AddStudentFromResultsActivity"
            android:label="Update Student"/>

        <activity android:name=".Setting"
            android:label="Setting"/>


    </application>

</manifest>